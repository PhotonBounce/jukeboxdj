This folder is populated at build time by .github/workflows/build-apk-jukeboxdj.yml,
which copies projects/jukeboxdj/ here so the APK bundles the whole web app.
Nothing else should be committed in this directory.
